import { useEffect } from "react";

const StockManagement = () => {
  useEffect(() => {
    window.location.href = "https://optera-insight.lovable.app/";
  }, []);

  return null;
};

export default StockManagement;
