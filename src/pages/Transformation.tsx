import { useEffect } from "react";

const Transformation = () => {
  useEffect(() => {
    window.location.href = "https://transform-the-spark.lovable.app";
  }, []);

  return null;
};

export default Transformation;
